---
title: "{{ replace .Name "-" " " | title }}"
date: {{ .Date }}
draft: true
type: "page"
# Banner 配置（可选，不设置则使用全局配置）
# banner_img: "/img/default.png"
# banner_img_height: 60
# banner_mask_alpha: 0.3
# 副标题（可选，默认使用标题）
# subtitle: ""
# 是否启用评论（默认关闭）
comments: false
# 是否启用数学公式（默认关闭）
math: false
# 是否启用 Mermaid 流程图（默认关闭）
mermaid: false
# 是否启用图片放大（默认跟随全局配置）
# image_zoom: true
---

